var textContent = document.getElementById("text-content");
var boldBtn = document.querySelector(".bold");
var editText = () => {
    if (cond) {
        
    }
    textContent.style.fontWeight = "bold";

}

boldBtn.addEventListener("click", editText);